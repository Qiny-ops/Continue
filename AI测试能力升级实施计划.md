# AI 测试能力升级实施计划

> 基于《2026年，测试工程师必须看懂的12个AI测试热词》梳理，结合项目现有能力现状，制定分阶段实施计划。

## 现状盘点

| 热词 | 当前状态 | 说明 |
|---|---|---|
| LLM Testing | 已有 | ai-generator-service 按测试方向并行生成用例；Django 侧编排需求提取→关联检索→AI生成→保存全流程，含 SSE 流式推送、重试、KTO 数据集、向量去重 |
| RAG Testing | 已有 | RAG_kb 具备完整混合检索（BM25+向量+RRF融合+Rerank+智能去重）；Django 侧通过 RequirementService 从知识库提取需求并检索关联内容；api-testing-service 通过 KBClient 调用知识库会话 |
| MCP | 已有 | api-testing-service 暴露 6 个 MCP Tools（get_api_flow / generate_testcases / get_api_dependency / fill_testdata / execute_testcase / validate_testcase），通过 FastMCP stdio 传输 |
| Test Agent / Agentic Testing | 部分有 | RAG_kb 的 AgentService 实现了 ReAct 模式（思考→行动→观察循环），支持知识检索/计算器/代码执行/网络搜索/时间工具，但未与测试流程打通 |
| 多模态测试 | 部分有 | RAG_kb 支持 4 种 Vision Provider（OpenAI/智谱/通义/Ollama），但未接入测试用例生成流程 |
| LLM-as-Judge | 无 | 无 AI 辅助评审机制，用例评审仅靠人工 |
| MECE 拆解 | 无 | 当前按"功能点/业务逻辑/其他"三个方向生成，缺乏结构化维度约束 |
| AI 幻觉识别 | 无 | 无幻觉检测/标注机制 |
| Vibe Testing | 无 | 无自然语言→测试意图→自动执行闭环 |
| 自愈测试 | 无 | 无 UI 定位修复能力 |
| Mutation Testing | 无 | 无变异测试能力 |

---

## 阶段一：让生成结果更结构化、更贴近业务

**核心问题**：当前 AI 生成的用例"散"——看似正确但缺少整体覆盖逻辑；"偏"——对业务上下文理解不够，容易遗漏关键场景。

**目标**：从"能生成"走向"生成得准、生成得全"

### 1.1 MECE 维度定义与约束

**问题**：当前 ai-generator-service 的 AIGenerationService.TEST_PROMPTS 只有三个粗粒度方向（功能点/业务逻辑/其他），AI 想到哪写到哪，无法保证覆盖的完整性和正交性。

**方案**：
- 在 ai-generator-service 的 prompt 层引入 MECE 结构化拆解维度，替代现有三个方向
- 拆解维度定义：功能维度（正向/逆向/边界值）、数据维度（等价类/边界值/特殊字符/空值）、状态维度（初始态/中间态/终态/状态转换）、权限维度（有权限/无权限/越权/角色交叉）、异常维度（超时/并发/资源不可用/依赖失败）
- 每个维度生成前，prompt 中明确该维度的覆盖要求，约束 AI 按维度输出
- 输出格式要求每条用例标注所属维度，便于后续覆盖度检查

**涉及服务**：ai-generator-service（prompts 模块、InferenceEngine）

**验收标准**：生成的用例列表中每条用例带有 dimension 标签；按维度统计后无空维度

### 1.2 MECE 覆盖度检查与补充生成

**问题**：即使定义了维度，AI 仍可能在某些维度上生成 0 条用例，导致覆盖盲区。

**方案**：
- 生成后增加覆盖度检查步骤：统计每个维度的用例数量
- 对用例数为 0 或明显偏少的维度，自动触发补充生成（带维度约束的 prompt）
- 补充生成的用例追加到结果列表中，标注 generation_type: "supplement"
- 前端展示覆盖度矩阵（维度 × 模块），直观显示覆盖情况

**涉及服务**：ai-generator-service（InferenceEngine.generate_testcases）、Django AIGenerationService、vue3-frontend

**依赖**：1.1

**验收标准**：所有 MECE 维度至少有 1 条用例；前端可查看覆盖度矩阵

### 1.3 RAG 上下文增强——扩展检索源

**问题**：当前需求检索只从 PRD 文档中获取上下文，AI 看不到历史缺陷、已有用例覆盖情况，容易重复或遗漏。

**方案**：
- Django 侧 AIGenerationService 的需求检索阶段，除 PRD 外增加检索源：
  - 历史用例库：检索已有用例，避免重复生成，识别覆盖盲区
  - 缺陷库：检索历史缺陷，针对缺陷高发区域加深测试
  - 接口文档：检索接口定义，确保用例的接口参数与实际一致
- 检索结果按类型标注来源，拼入 AI 生成上下文
- 在 prompt 中增加指令："以下为历史缺陷摘要，请针对这些缺陷场景补充测试用例"

**涉及服务**：Testbackend（knowledge/requirement_service）、RAG_kb

**验收标准**：AI 生成上下文中包含至少 2 种来源的信息；生成的用例能体现对历史缺陷的针对性

### 1.4 知识库文档类型标注

**问题**：RAG_kb 的文档上传无类型区分，检索时无法按文档类型加权，PRD 和缺陷报告混在一起。

**方案**：
- RAG_kb 的文档上传 API 增加 doc_type 字段，支持类型：PRD / API_DOC / TEST_CASE / BUG_REPORT / INCIDENT_REPORT / OTHER
- 文档模型（Document）增加 doc_type 列
- 检索时支持按 doc_type 过滤和加权：PRD 和 API_DOC 权重较高，BUG_REPORT 在缺陷驱动的检索中权重提升
- 前端上传时提供类型选择下拉

**涉及服务**：RAG_kb（models/document、api/documents、services/document_service）、vue3-frontend

**验收标准**：上传文档时可选择类型；检索结果中包含类型标签；按类型过滤检索生效

### 1.5 多模态需求输入——原型图/设计稿接入

**问题**：真实项目中很多关键细节藏在原型图和设计稿中，当前只能处理文本，无法从视觉信息中提取测试点。

**方案**：
- 利用 RAG_kb 已有的 Vision Provider 能力（OpenAI/智谱/通义/Ollama），在测试用例生成流程中增加"图片理解"步骤：
  1. 用户上传原型图/设计稿/流程图
  2. Vision Provider 提取图片中的页面结构、交互元素、业务流程等测试相关信息
  3. 提取结果作为文本拼入 RAG 检索上下文
  4. 后续按正常流程生成测试用例
- Django 侧 AIGenerationService 增加"从图片提取需求"的入口
- 前端 AI 生成页面增加图片上传区域

**涉及服务**：RAG_kb（vision providers）、Testbackend（ai_generation_service）、vue3-frontend

**依赖**：1.3

**验收标准**：上传原型图后能提取出页面元素和交互信息；生成的用例中包含从图片中提取的测试场景

---

## 阶段二：建立生成结果的质量评估体系

**核心问题**：AI 一次生成几十上百条用例，人工逐条评审成本很高，且容易遗漏问题用例。

**目标**：从"人工逐条审"走向"AI 初筛 + 人工复核"

### 2.1 LLM-as-Judge 评审服务

**问题**：无 AI 辅助评审机制，用例评审完全依赖人工。

**方案**：
- 在 ai-generator-service 新增 /api/v1/inference/judge 端点
- 评审维度定义：
  - 覆盖度：是否覆盖了需求中的所有功能点
  - 合理性：测试步骤和预期结果是否逻辑自洽
  - 边界完整性：是否考虑了边界值、异常值、空值
  - 断言强度：预期结果是否可验证、是否足够具体
  - 可执行性：步骤描述是否足够清晰，能被执行
- 输出格式：每条用例返回 score（0-100）+ 问题标注 + 修改建议
- 支持批量评审（一次评审一个模块的所有用例）

**涉及服务**：ai-generator-service（routers/inference、prompts、新增 schemas）

**验收标准**：调用评审端点返回每条用例的评分和问题标注；评审耗时 < 用例数 × 2秒

### 2.2 幻觉检测标注

**问题**：AI 生成的用例可能引用需求中不存在的字段、接口或规则，且具有很强的"专业外观"，人工难以识别。

**方案**：
- 在评审流程中增加幻觉识别步骤：
  1. 将 AI 生成的用例与原始需求文档做交叉验证
  2. 检查用例中引用的字段名是否存在于接口文档
  3. 检查用例中假设的规则是否在 PRD 中有依据
  4. 检查用例中引用的状态值是否在业务规则中定义
- 标注结果分类：confirmed（有据可查）、suspected_hallucination（疑似幻觉，无依据）、unverifiable（无法验证）
- 疑似幻觉的用例高亮标注，给出"依据缺失"的具体说明

**涉及服务**：ai-generator-service（InferenceEngine 新增幻觉检测方法）、RAG_kb（提供原文检索能力做交叉验证）

**依赖**：2.1

**验收标准**：评审结果中包含幻觉标注；能检出"引用了不存在的接口字段"类幻觉

### 2.3 分层评审策略

**问题**：所有用例平等对待，评审效率低；关键场景和普通场景应有不同的评审深度。

**方案**：
- 三层评审策略：
  - L1（AI 自动通过）：score ≥ 80 且无幻觉标注的用例，AI 自动标记为"建议通过"，人工可跳过
  - L2（AI 初筛 + 人工快速复核）：score 60-79 或有疑似幻觉标注的用例，AI 给出问题摘要，人工快速确认
  - L3（强制人工复核）：关键业务场景（金融/安全/合规相关）、score < 60、或标注为 confirmed hallucination 的用例
- 评审分层规则可在前端配置（按业务场景/模块/优先级定义哪些走 L3）
- Django 侧 TestCase 模型增加 review_tier 字段

**涉及服务**：ai-generator-service（评审逻辑）、Testbackend（TestCase 模型、review_service）、vue3-frontend

**依赖**：2.1, 2.2

**验收标准**：评审结果按 L1/L2/L3 分层；L1 用例可一键批量通过；L3 用例不可跳过人工复核

### 2.4 评审结果持久化与前端展示

**问题**：评审结果只在生成时展示，后续查看用例时无法看到评审信息。

**方案**：
- Django 侧 TestCase 模型增加字段：
  - ai_score（AI 评分）
  - hallucination_flags（幻觉标注，JSON）
  - review_tier（评审层级）
  - ai_review_notes（AI 评审备注，JSON）
- 前端用例列表页增加评审状态列，支持按评审层级/评分筛选
- 用例详情页展示评审详情：评分、问题标注、修改建议
- 评审不通过的用例可一键"采纳 AI 修改建议"或手动修改

**涉及服务**：Testbackend（TestCase 模型、serializers）、vue3-frontend

**依赖**：2.1, 2.2

**验收标准**：用例列表和详情页展示 AI 评审信息；支持按评审层级筛选

### 2.5 评审反馈闭环

**问题**：人工复核的结果未回馈到模型优化，同样的问题会反复出现。

**方案**：
- 人工复核操作（确认幻觉/修正用例/驳回用例/采纳建议）回写 KTO 数据集：
  - 确认幻觉 → 写入负样本（AI 输出被人工否决）
  - 人工修正后通过 → 写入正样本（修正后的内容作为 chosen）
  - 采纳建议 → 写入正样本（AI 建议被接受）
- 定期统计幻觉高频模式（如"编造接口字段"占比最高），针对性优化 prompt
- 长期积累后可用于微调模型

**涉及服务**：Testbackend（ai_generation_service 的 KTO 逻辑）、ai-generator-service

**依赖**：2.3, 2.4

**验收标准**：人工复核操作自动写入 KTO 数据集；可查看幻觉高频模式统计

---

## 阶段三：AI 深入工程流程

**核心问题**：当前 AI 只参与了"生成"和"执行"两个环节，与工程工具链（Git/CI/CD/缺陷管理）的连接需要人工中转。

**目标**：从"AI 生成用例"走向"AI 参与执行、维护、调试"

### 3.1 MCP 工具扩展——连接工程工具链

**问题**：api-testing-service 的 MCP Server 只暴露了知识库和测试执行相关工具，无法读取代码变更、CI 结果、缺陷信息。

**方案**：
- api-testing-service 的 MCP Server 新增工具：
  - git_diff：读取指定仓库的 Git diff，返回变更的文件和接口列表
  - ci_status：查询指定分支的 CI/CD 构建状态和测试结果
  - bug_search：查询缺陷管理系统中指定模块/优先级的缺陷列表
  - coverage_report：查询指定模块的代码覆盖率数据
- 每个工具封装为 FastMCP tool，支持配置连接参数（Git 仓库地址、CI API 地址等）

**涉及服务**：api-testing-service（mcp_server.py）

**验收标准**：通过 MCP 调用可获取 Git diff 和 CI 状态；新增工具在 Claude Desktop 等 MCP 客户端中可用

### 3.2 变更影响分析与回归推荐

**问题**：代码变更后，测试人员难以快速判断哪些接口/模块受影响，需要回归哪些用例。

**方案**：
- 基于 git_diff 工具返回的变更信息，AI 分析变更影响范围：
  1. 识别变更涉及的接口和模块
  2. 在知识库中检索与变更相关的测试用例
  3. 分析变更类型（新增接口/修改参数/删除字段），推荐回归策略
  4. 输出影响范围报告 + 推荐回归用例列表
- 前端增加"变更影响分析"入口，输入 Git commit/分支，输出分析报告

**涉及服务**：api-testing-service（新增影响分析引擎）、vue3-frontend

**依赖**：3.1

**验收标准**：输入 Git 变更后输出受影响接口列表和推荐回归用例

### 3.3 自愈测试——接口层

**问题**：接口测试因字段变更/响应结构变化导致失败时，需要人工排查并修改用例，维护成本高。

**方案**：
- 当接口测试执行失败时，AI 自动分析失败原因：
  1. 对比实际响应与预期响应的差异
  2. 判断差异类型：字段名变更 / 字段类型变更 / 新增字段 / 删除字段 / 值域变更
  3. 对非语义变更（字段名变更、新增可选字段），自动生成修复建议
  4. 对语义变更（业务规则变化），标记为"需人工确认"
- 修复后的用例标记为"自愈修复"，需人工确认后才正式生效
- 自愈操作记录审计日志

**涉及服务**：api-testing-service（TestExecutionEngine 增加 self-heal 逻辑、MCP 新增 selfheal_testcase 工具）

**验收标准**：接口字段名变更导致的失败可自动修复；修复后的用例标注"自愈修复"需人工确认

### 3.4 Vibe Testing——自然语言测试意图入口

**问题**：测试人员必须通过知识库选择文档→触发生成，无法直接用自然语言表达测试意图。

**方案**：
- 前端增加"测试意图"输入框，支持自然语言描述，如：
  - "测试登录流程的各种异常情况"
  - "对用户注册模块做安全和权限方面的测试"
  - "这个接口的边界值场景都要覆盖到"
- 系统处理流程：
  1. AI 解析测试意图，提取测试目标、模块、维度等结构化信息
  2. 自动匹配知识库中的相关文档
  3. 结合 MECE 维度拆解为具体测试用例
  4. 生成后可选直接执行
- 将测试意图与生成结果关联保存，积累为"测试意图模板库"

**涉及服务**：vue3-frontend（新增意图输入组件）、ai-generator-service（新增意图解析端点）、api-testing-service（可选执行）

**依赖**：1.1（MECE 维度）

**验收标准**：输入自然语言测试意图后可生成对应维度的用例；意图解析结果包含模块和维度标签

### 3.5 执行结果自动归档

**问题**：测试执行结果散落在各处，未与用例版本关联，无法形成"生成→执行→结果→优化"的闭环。

**方案**：
- api-testing-service 的执行结果回调 Django 侧，自动关联到用例版本：
  - 执行通过 → 更新用例状态为 passed
  - 执行失败 → 更新用例状态为 failed，记录失败原因
  - 自愈修复 → 更新用例内容，记录修复历史
- Django 侧 TestCaseVersion 模型增加执行结果关联
- 前端用例详情页可查看历史执行趋势（通过率变化曲线）

**涉及服务**：api-testing-service（执行结果回调）、Testbackend（TestCase 模型、execution_service）、vue3-frontend

**验收标准**：执行结果自动关联到用例；用例详情页可查看执行历史

---

## 阶段四：AI 自主规划和执行

**核心问题**：当前每个环节（生成/评审/执行/校验）都需要人工触发和串联，AI 只是单步工具。

**目标**：从"人指挥 AI 做单步"走向"人定义目标，AI 自主规划执行"

### 4.1 Test Agent 框架搭建

**问题**：RAG_kb 的 AgentService 已有 ReAct 模式，但未注册测试专用工具，未与测试流程打通。

**方案**：
- 基于 RAG_kb AgentService 的 ReAct 框架，构建 TestAgent：
  - 注册测试专用工具集：
    - requirement_search：需求检索（复用 RAG_kb 的知识检索）
    - testcase_generate：用例生成（调用 ai-generator-service）
    - testcase_judge：用例评审（调用评审端点）
    - api_execute：接口执行（调用 api-testing-service）
    - result_validate：结果校验（调用校验端点）
    - bug_report：缺陷报告（新建或对接缺陷系统）
  - Agent 系统提示词定义测试专家角色
  - 支持配置最大迭代次数和工具白名单

**涉及服务**：新建 test-agent-service 或扩展 RAG_kb

**依赖**：阶段一~三的核心能力（生成/评审/执行/校验端点就绪）

**验收标准**：Test Agent 可通过 ReAct 循环调用至少 4 个测试工具完成任务

### 4.2 测试任务自主规划

**问题**：测试任务需要人工拆解为多个步骤分别执行，无法给定一个目标后自动完成。

**方案**：
- Test Agent 接收测试目标（如"对用户注册模块做全面测试"），自主规划：
  1. 思考：分析目标，确定测试范围
  2. 行动：调用 requirement_search 检索需求
  3. 思考：确定需要的用例维度
  4. 行动：调用 testcase_generate 生成用例
  5. 思考：评审生成结果
  6. 行动：调用 testcase_judge 评审用例质量
  7. 思考：执行测试
  8. 行动：调用 api_execute 执行测试
  9. 思考：校验结果
  10. 行动：调用 result_validate 校验结果
  11. 思考：汇总报告
  12. 行动：FINISH，输出测试报告
- 支持自然语言输入目标，Agent 自主决定调用哪些工具、调用顺序

**涉及服务**：Test Agent

**依赖**：4.1

**验收标准**：输入"测试XX模块"后 Agent 自主完成检索→生成→评审→执行→校验→报告全流程

### 4.3 执行策略动态调整

**问题**：当前测试执行是静态的——预先生成所有用例再逐条执行，无法根据中间结果调整策略。

**方案**：
- Test Agent 在执行过程中根据中间结果动态调整：
  - 某接口返回异常 → 自动追加该接口的边界值用例
  - 某模块通过率低 → 加深该模块的测试，增加异常维度用例
  - 某接口响应超时 → 增加性能/并发测试用例
- 动态调整需要遵守预算约束：最大用例数、最大执行时间
- 调整决策过程记录在 Agent 思考日志中，便于回溯

**涉及服务**：Test Agent

**依赖**：4.2

**验收标准**：Agent 执行中遇到异常可自动追加用例；调整决策有日志可查

### 4.4 风险边界与治理

**问题**：AI 越主动，越需要明确的操作边界和治理机制，避免不可控行为。

**方案**：
- 定义 Agent 操作边界：
  - 允许的操作：生成用例、执行接口测试、读取知识库、分析结果
  - 禁止的操作：修改生产数据、删除用例（仅标记）、直接关闭缺陷
  - 需人工确认的操作：自愈修复用例、标记用例为"通过"、创建缺陷
- 治理机制：
  - 每步操作记录审计日志（工具名、参数、结果、时间戳）
  - 关键操作（如修改用例内容）需人工审批
  - 执行超时自动停止并报告当前状态
  - Agent 操作权限可通过前端配置

**涉及服务**：Test Agent（审计日志模块）、vue3-frontend（权限配置页）

**依赖**：4.2

**验收标准**：Agent 所有操作有审计日志；禁止操作被拦截；关键操作需人工审批

### 4.5 Agent 执行看板

**问题**：Agent 自主执行时，测试人员无法了解当前进度和中间结果，缺乏掌控感。

**方案**：
- 前端新增 Agent 执行看板页面：
  - 实时展示当前步骤和思考过程
  - 工具调用时间线（调用什么工具、参数是什么、返回了什么）
  - 中间结果摘要（已生成N条用例、已执行N个接口、通过率X%）
  - 支持人工随时暂停/继续/接管
  - Agent 完成后展示测试报告
- 通过 SSE 推送 Agent 执行进度

**涉及服务**：vue3-frontend（Agent 看板组件）、Test Agent（SSE 进度推送）

**依赖**：4.2

**验收标准**：看板实时展示 Agent 执行状态；支持暂停/继续/接管

---

## 阶段五：持续优化与度量

**核心问题**：测试做完就结束了，缺少对"测试本身是否有效"的验证，缺少全流程的效能度量。

**目标**：从"做完测试"走向"持续验证测试本身的有效性"

### 5.1 Mutation Testing——接口层变异测试

**问题**：传统的代码覆盖率只能说明代码是否被执行到，不能说明测试是否能发现错误。

**方案**：
- 对接口参数/响应结构做变异：
  - 替换必填字段为空值
  - 篡改枚举值为非法值
  - 越界数值（负数、超大值、0）
  - 删除必填字段
  - 注入非法字符（SQL注入、XSS）
- 变异后的接口作为"缺陷注入"，用现有测试用例执行，观察是否能发现变异
- 变异测试结果：killed（用例发现了变异）/ survived（用例未发现变异）

**涉及服务**：api-testing-service（新增 Mutator 模块和 mutation testing 端点）

**验收标准**：可对指定接口执行变异测试；输出 killed/survived 统计

### 5.2 用例有效性评分

**问题**：不知道哪些用例是"永远通过的废用例"，哪些用例真正有缺陷发现能力。

**方案**：
- 基于变异测试结果计算每条用例的"缺陷发现能力"分数：
  - mutation_score = killed_mutations / total_mutations
  - 长期通过但 mutation_score 为 0 的用例标记为"低效用例"
- 评分维度综合：
  - 变异杀死率（来自 Mutation Testing）
  - 历史缺陷关联度（用例是否曾发现真实缺陷）
  - 执行稳定性（用例是否经常因环境问题失败）
- 低效用例建议：补充断言、删除或重构

**涉及服务**：api-testing-service（评分逻辑）、Testbackend（用例评分字段）

**依赖**：5.1

**验收标准**：每条用例有有效性评分；可识别"永远通过的废用例"

### 5.3 效能度量仪表盘

**问题**：缺少 AI 测试全流程的效能指标，无法量化 AI 的实际贡献。

**方案**：
- 前端新增效能度量仪表盘，展示核心指标：
  - 生成阶段：生成用例总数 / 评审通过率 / 幻觉检出率 / 平均生成耗时
  - 执行阶段：执行用例总数 / 通过率 / 自愈修复率 / 人工介入率
  - 质量阶段：变异杀死率 / 缺陷发现数 / 低效用例占比
  - 趋势：各指标随时间的变化曲线
- 支持按项目/模块/时间范围筛选

**涉及服务**：vue3-frontend（仪表盘组件）、Testbackend（度量数据聚合 API）

**依赖**：阶段一~四

**验收标准**：仪表盘展示核心效能指标；支持按项目/时间筛选

### 5.4 用例资产沉淀

**问题**：高质量用例与低质量用例混在一起，无法区分"资产"和"临时"用例。

**方案**：
- 定义"资产用例"标准：
  - 有效性评分 ≥ 阈值（来自 5.2）
  - 至少发现过 1 次真实缺陷或杀死过变异
  - 评审通过且无幻觉标注
  - 执行稳定（近 10 次执行无环境性失败）
- 满足标准的用例自动标记为"资产用例"，纳入回归基线
- 不满足标准且长期未修改的低效用例，定期提醒清理
- 资产用例支持跨版本复用

**涉及服务**：Testbackend（用例标记逻辑、资产用例 API）、vue3-frontend

**依赖**：5.2

**验收标准**：高质量用例自动标记为资产用例；资产用例纳入回归基线

---

## 实施优先级与里程碑

| 优先级 | 阶段 | 预计周期 | 理由 |
|---|---|---|---|
| P0 | 阶段一：MECE + RAG 深化 | 3-4 周 | 当前生成"散"和"偏"的问题最直接影响使用体验，投入产出比最高 |
| P1 | 阶段二：LLM-as-Judge + 幻觉识别 | 2-3 周 | 生成量上来后人工评审是瓶颈，不解决会限制规模化使用 |
| P2 | 阶段三：MCP 扩展 + Vibe Testing | 3-4 周 | 让 AI 从"只生成"走向"参与工程"，提升端到端效率 |
| P3 | 阶段四：Test Agent | 4-6 周 | 依赖前三阶段的基础能力，是能力跃升但复杂度最高 |
| P4 | 阶段五：Mutation + 度量 | 2-3 周 | 长期质量保障，在前四阶段稳定后推进 |

### 里程碑节点

- **M1**：MECE 维度约束生效，生成用例按维度标注，覆盖度矩阵可视化
- **M2**：LLM-as-Judge 评审服务上线，分层评审策略落地，人工评审效率提升 50%+
- **M3**：MCP 工具链打通，Vibe Testing 入口可用，自愈测试对接口层生效
- **M4**：Test Agent 可自主完成"目标→规划→执行→报告"全流程
- **M5**：变异测试上线，效能度量仪表盘可视化，用例资产沉淀机制运转
