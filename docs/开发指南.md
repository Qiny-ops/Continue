# 测试用例管理系统 - 开发文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 测试用例管理系统 |
| 文档版本 | V1.0 |
| 创建日期 | 2024年 |
| 文档状态 | 正式发布 |

---

## 一、开发环境搭建

### 1.1 环境要求

#### 前端环境

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | ^20.19.0 或 >=22.12.0 | JavaScript 运行时 |
| pnpm | 最新版 | 包管理器 |
| VS Code | 最新版 | 推荐编辑器 |

#### 后端环境

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| Python | 3.13 | 编程语言 |
| pip | 最新版 | 包管理器 |
| virtualenv | 最新版 | 虚拟环境 |

### 1.2 项目克隆

```bash
# 克隆项目
git clone <repository-url>
cd Continue
```

### 1.3 后端环境配置

```bash
# 创建虚拟环境
python -m venv myenv

# 激活虚拟环境 (Windows)
myenv\Scripts\activate

# 激活虚拟环境
source myenv/bin/activate

# 安装依赖
pip install django djangorestframework django-cors-headers django-filter pyjwt requests

# 进入后端目录
cd Testbackend

# 数据库迁移
python manage.py makemigrations
python manage.py migrate

# 创建超级用户
python manage.py createsuperuser

# 启动开发服务器
python manage.py runserver
```

### 1.4 前端环境配置

```bash
# 进入前端目录
cd vue3-frontend

# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览生产构建
pnpm preview
```

### 1.5 IDE 配置

#### VS Code 推荐插件

```json
{
  "recommendations": [
    "Vue.volar",
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "ms-python.python",
    "ms-python.vscode-pylance"
  ]
}
```

#### VS Code 设置

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "[python]": {
    "editor.defaultFormatter": "ms-python.python"
  },
  "[vue]": {
    "editor.defaultFormatter": "Vue.volar"
  }
}
```

---

## 二、项目结构详解

### 2.1 前端项目结构

```
vue3-frontend/
├── public/                     # 静态资源目录
│   └── logo.png               # 网站图标
│
├── src/                       # 源代码目录
│   ├── api/                   # API 接口层
│   │   ├── axios.js          # Axios 实例和拦截器配置
│   │   ├── index.js          # 通用 API 实例
│   │   ├── auth.js           # 用户认证相关 API
│   │   ├── project.js        # 项目管理相关 API
│   │   ├── testcase.js       # 测试用例相关 API
│   │   └── member.js         # 成员管理相关 API
│   │
│   ├── composables/           # 组合式函数 (Composables)
│   │   ├── useAuth.js        # 认证相关逻辑复用
│   │   └── useTestCaseApi.js # 测试用例 API 复用
│   │
│   ├── constants/             # 常量定义
│   │   └── testcase.js       # 测试用例相关常量
│   │
│   ├── layout/                # 布局组件
│   │   ├── AppLayout.vue     # 应用主布局
│   │   └── NavigationBar.vue # 顶部导航栏
│   │
│   ├── models/                # 数据模型类
│   │   └── project.js        # 项目模型类定义
│   │
│   ├── router/                # 路由配置
│   │   ├── index.js          # 路由主配置文件
│   │   ├── guards.js         # 路由守卫
│   │   └── modules/          # 路由模块
│   │       ├── auth.js       # 认证相关路由
│   │       └── project.js    # 项目相关路由
│   │
│   ├── stores/                # Pinia 状态管理
│   │   ├── api.js            # API 请求状态
│   │   ├── counter.js        # 示例 Store
│   │   ├── auth/             # 认证状态模块
│   │   │   └── user.js       # 用户状态管理
│   │   ├── layout/           # 布局状态模块
│   │   │   └── nav.js        # 导航状态管理
│   │   └── projects/         # 项目状态模块
│   │       ├── project.js    # 项目状态管理
│   │       └── member.js     # 成员状态管理
│   │
│   ├── utils/                 # 工具函数
│   │   ├── debounce.js       # 防抖函数
│   │   ├── cache/            # 缓存工具
│   │   │   ├── nav-cache.js  # 导航缓存
│   │   │   └── request-cache.js # 请求缓存
│   │   └── responsive/       # 响应式工具
│   │       └── device-utils.js # 设备检测工具
│   │
│   ├── views/                 # 页面组件
│   │   ├── auth/             # 认证相关页面
│   │   │   ├── LoginPage.vue # 登录页面
│   │   │   ├── RegisterPage.vue # 注册页面
│   │   │   ├── ForgotPassword.vue # 忘记密码页面
│   │   │   └── UserInfo.vue  # 用户信息页面
│   │   ├── projects/         # 项目相关页面
│   │   │   ├── ProjectList.vue # 项目列表页面
│   │   │   ├── ProjectCard.vue # 项目卡片组件
│   │   │   ├── ProjectDetail.vue # 项目详情页面
│   │   │   ├── CreateProject.vue # 创建项目页面
│   │   │   └── ProjectEmpty.vue # 空状态组件
│   │   └── testcase/         # 测试用例相关页面
│   │       ├── TestCaseGrid.vue # 用例网格视图
│   │       ├── TestCaseTable.vue # 用例表格视图
│   │       ├── ModuleTree.vue # 模块树组件
│   │       └── ImportDialog.vue # 导入对话框
│   │
│   ├── App.vue               # 根组件
│   ├── main.js               # 应用入口
│   ├── auto-imports.d.ts     # 自动导入类型声明
│   └── components.d.ts       # 组件类型声明
│
├── dist/                      # 构建输出目录
│
├── .vscode/                   # VS Code 配置
│   ├── extensions.json       # 推荐插件
│   └── settings.json         # 编辑器设置
│
├── vite.config.js            # Vite 配置文件
├── package.json              # 项目配置和依赖
├── pnpm-lock.yaml            # 依赖锁定文件
├── jsconfig.json             # JavaScript 配置
├── eslint.config.js          # ESLint 配置
├── .prettierrc.json          # Prettier 配置
├── .editorconfig             # 编辑器配置
└── README.md                 # 项目说明
```

### 2.2 后端项目结构

```
Testbackend/
├── Testbackend/               # Django 项目配置
│   ├── __init__.py
│   ├── settings.py           # 项目设置
│   ├── urls.py               # 主路由配置
│   ├── asgi.py               # ASGI 应用入口
│   └── wsgi.py               # WSGI 应用入口
│
├── apps/                      # Django 应用目录
│   ├── __init__.py
│   │
│   ├── users/                # 用户应用
│   │   ├── __init__.py
│   │   ├── models.py         # 用户、角色、权限模型
│   │   ├── views.py          # 用户认证视图
│   │   ├── urls.py           # 用户路由
│   │   ├── authentication.py # JWT 认证类
│   │   ├── admin.py          # Admin 配置
│   │   ├── tests.py          # 单元测试
│   │   └── migrations/       # 数据库迁移
│   │
│   ├── projects/             # 项目应用
│   │   ├── __init__.py
│   │   ├── models.py         # 项目、项目成员模型
│   │   ├── views.py          # 项目管理视图
│   │   ├── urls.py           # 项目路由
│   │   ├── apps.py           # 应用配置
│   │   └── migrations/       # 数据库迁移
│   │
│   └── testcase/             # 测试用例应用
│       ├── __init__.py
│       ├── models.py         # 用例库、版本、模块、用例模型
│       ├── views.py          # 用例管理视图
│       ├── serializers.py    # 序列化器
│       ├── urls.py           # 用例路由
│       ├── admin.py          # Admin 配置
│       ├── apps.py           # 应用配置
│       └── migrations/       # 数据库迁移
│
├── scripts/                   # 脚本目录
│   └── generate_test_data.py # 测试数据生成脚本
│
├── venv/                      # Python 虚拟环境
│
├── manage.py                  # Django 管理脚本
├── db.sqlite3                 # SQLite 数据库文件
└── *.py                       # 其他脚本文件
```

---

## 三、前端开发指南

### 3.1 技术栈详解

#### Vue 3 组合式 API

```javascript
// 使用 setup 语法糖
<script setup>
import { ref, computed, onMounted } from 'vue'
import { useUserStore } from '@/stores/auth/user'

const userStore = useUserStore()
const loading = ref(false)

const isLoggedIn = computed(() => userStore.isLoggedIn)

onMounted(async () => {
  loading.value = true
  await userStore.fetchUserInfo()
  loading.value = false
})
</script>
```

#### Pinia 状态管理

```javascript
// stores/auth/user.js
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useUserStore = defineStore('user', () => {
  const user = ref(null)
  const isLoggedIn = computed(() => !!user.value)
  
  const login = async (credentials) => {
    // 登录逻辑
  }
  
  return {
    user,
    isLoggedIn,
    login
  }
})
```

#### Vue Router

```javascript
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  { path: '/login', component: () => import('@/views/auth/LoginPage.vue') },
  { path: '/projects', component: () => import('@/views/projects/ProjectList.vue'), meta: { requiresAuth: true } }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

### 3.2 API 封装规范

#### Axios 实例配置

```javascript
import axios from 'axios'

const instance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

instance.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => Promise.reject(error)
)

instance.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error.response?.data || { message: '请求失败' })
  }
)

export default instance
```

#### API 模块封装

```javascript
import axios from '@/api/axios.js'

const BASE_URL = '/api/projects'

const projectApi = {
  async getProjects(params = {}) {
    return axios.get(BASE_URL, { params })
  },
  
  async getProject(id) {
    return axios.get(`${BASE_URL}/${id}`)
  },
  
  async createProject(data) {
    return axios.post(`${BASE_URL}/create/`, data)
  },
  
  async updateProject(id, data) {
    return axios.put(`${BASE_URL}/${id}/update/`, data)
  },
  
  async deleteProject(id) {
    return axios.delete(`${BASE_URL}/${id}/delete/`)
  }
}

export default projectApi
```

### 3.3 组件开发规范

#### 组件命名

- 页面组件: PascalCase + Page.vue (如 `LoginPage.vue`)
- 业务组件: PascalCase.vue (如 `ProjectCard.vue`)
- 基础组件: Base + PascalCase.vue (如 `BaseButton.vue`)

#### 组件结构

```vue
<template>
  <div class="component-name">
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { useUserStore } from '@/stores/auth/user'

const props = defineProps({
  title: {
    type: String,
    required: true
  }
})

const emit = defineEmits(['update', 'delete'])

const userStore = useUserStore()
const loading = ref(false)

onMounted(() => {
})
</script>

<style scoped>
.component-name {
}
</style>
```

### 3.4 代码风格

#### ESLint 配置

```javascript
import js from '@eslint/js'
import pluginVue from 'eslint-plugin-vue'

export default [
  {
    name: 'app/vue',
    ...pluginVue.configs['flat/essential'],
    rules: {
      'vue/multi-word-component-names': 'off',
      'vue/no-unused-vars': 'error'
    }
  }
]
```

#### Prettier 配置

```json
{
  "semi": false,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "none",
  "printWidth": 100
}
```

---

## 四、后端开发指南

### 4.1 Django 配置详解

#### settings.py 关键配置

```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'corsheaders',
    'rest_framework',
    'django_filters',
    'apps.users',
    'apps.projects',
    'apps.testcase'
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

CORS_ALLOWED_ORIGINS = [
    "http://localhost:5173",
    "http://localhost:5174",
    "http://127.0.0.1:5173",
    "http://127.0.0.1:5174",
]

AUTH_USER_MODEL = "users.User"

JWT_SECRET = os.environ.get('JWT_SECRET', SECRET_KEY)
JWT_EXPIRATION_HOURS = int(os.environ.get('JWT_EXPIRATION_HOURS', 24))
```

### 4.2 模型开发规范

#### 模型定义

```python
from django.db import models
from django.contrib.auth.models import AbstractUser

class User(AbstractUser):
    STATUS_CHOICES = [
        ('active', '活跃'),
        ('disabled', '禁用'),
        ('suspended', '暂停'),
        ('pending', '待激活'),
    ]
    
    name = models.CharField(max_length=100, verbose_name='用户姓名')
    phone = models.CharField(max_length=20, blank=True, null=True, verbose_name='手机号码')
    status = models.CharField(max_length=20, choices=STATUS_CHOICES, default='active', verbose_name='状态')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='创建时间')
    updated_at = models.DateTimeField(auto_now=True, verbose_name='更新时间')
    
    class Meta:
        verbose_name = '用户'
        verbose_name_plural = '用户'
        db_table = 'users'
        ordering = ['-created_at']
    
    def __str__(self):
        return f"{self.username} ({self.name})"
```

### 4.3 视图开发规范

#### 函数视图

```python
from django.http import JsonResponse
from django.views.decorators.csrf import csrf_exempt
import json

@csrf_exempt
def example_view(request):
    if request.method == 'GET':
        data = {'message': 'Hello, World!'}
        return JsonResponse({'code': 200, 'data': data})
    elif request.method == 'POST':
        try:
            body = json.loads(request.body)
            return JsonResponse({'code': 200, 'message': 'Success', 'data': body})
        except json.JSONDecodeError:
            return JsonResponse({'code': 400, 'message': 'Invalid JSON'})
    else:
        return JsonResponse({'code': 405, 'message': 'Method not allowed'})
```

#### 类视图 (ViewSets)

```python
from rest_framework import viewsets, status
from rest_framework.response import Response
from rest_framework.permissions import IsAuthenticated

class TestCaseViewSet(viewsets.ModelViewSet):
    queryset = TestCase.objects.all()
    serializer_class = TestCaseSerializer
    authentication_classes = [JWTAuthentication]
    permission_classes = [IsAuthenticated]
    filter_backends = [DjangoFilterBackend, SearchFilter, OrderingFilter]
    filterset_fields = ['version', 'module', 'priority']
    search_fields = ['title', 'precondition']
    ordering_fields = ['created_at', 'priority']
    ordering = ['-created_at']
    
    def perform_create(self, serializer):
        serializer.save(created_by=self.request.user)
    
    @action(detail=False, methods=['get'])
    def by_version(self, request):
        version_id = request.query_params.get('version')
        if not version_id:
            return Response({'error': 'version required'}, status=status.HTTP_400_BAD_REQUEST)
        test_cases = self.queryset.filter(version_id=version_id)
        serializer = self.get_serializer(test_cases, many=True)
        return Response(serializer.data)
```

### 4.4 序列化器开发

```python
from rest_framework import serializers
from .models import TestCase, TestStep

class TestStepSerializer(serializers.ModelSerializer):
    class Meta:
        model = TestStep
        fields = ['id', 'step_number', 'description', 'expected_result']

class TestCaseSerializer(serializers.ModelSerializer):
    steps = TestStepSerializer(many=True, read_only=True)
    module_name = serializers.CharField(source='module.name', read_only=True)
    created_by_name = serializers.CharField(source='created_by.name', read_only=True)
    
    class Meta:
        model = TestCase
        fields = [
            'id', 'title', 'module', 'module_name', 'version',
            'priority', 'tags', 'automation_status', 'precondition',
            'steps', 'created_by', 'created_by_name', 'created_at', 'updated_at'
        ]
        read_only_fields = ['created_by', 'created_at', 'updated_at']
```

### 4.5 URL 配置

```python
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from .views import TestCaseViewSet

router = DefaultRouter()
router.register(r'testcases', TestCaseViewSet, basename='testcase')

urlpatterns = [
    path('', include(router.urls)),
]
```

---

## 五、认证与授权

### 5.1 JWT 认证实现

#### 生成 Token

```python
import jwt
from datetime import datetime, timedelta
from django.conf import settings

def generate_jwt_token(user):
    expire_time = datetime.now() + timedelta(hours=settings.JWT_EXPIRATION_HOURS)
    payload = {
        'user_id': user.id,
        'username': user.username,
        'exp': expire_time
    }
    token = jwt.encode(payload, settings.JWT_SECRET, algorithm='HS256')
    return token
```

#### 验证 Token

```python
def verify_jwt_token(token):
    try:
        payload = jwt.decode(token, settings.JWT_SECRET, algorithms=['HS256'])
        return payload
    except jwt.ExpiredSignatureError:
        return None
    except jwt.InvalidTokenError:
        return None
```

#### JWT 认证装饰器

```python
def jwt_required(view_func):
    def wrapper(request, *args, **kwargs):
        auth_header = request.META.get('HTTP_AUTHORIZATION')
        if not auth_header:
            return JsonResponse({'code': 401, 'message': '缺少认证 token'})
        
        try:
            token = auth_header.split(' ')[1]
        except IndexError:
            return JsonResponse({'code': 401, 'message': 'Token 格式错误'})
        
        payload = verify_jwt_token(token)
        if not payload:
            return JsonResponse({'code': 401, 'message': 'Token 无效或已过期'})
        
        user_id = payload.get('user_id')
        try:
            user = User.objects.get(id=user_id)
            request.user = user
        except User.DoesNotExist:
            return JsonResponse({'code': 401, 'message': '用户不存在'})
        
        return view_func(request, *args, **kwargs)
    
    return wrapper
```

### 5.2 DRF 认证类

```python
from rest_framework import authentication, exceptions
from .models import User

class JWTAuthentication(authentication.BaseAuthentication):
    def authenticate(self, request):
        auth_header = request.META.get('HTTP_AUTHORIZATION')
        if not auth_header:
            return None
        
        try:
            token = auth_header.split(' ')[1]
        except IndexError:
            raise exceptions.AuthenticationFailed('Token 格式错误')
        
        payload = verify_jwt_token(token)
        if not payload:
            raise exceptions.AuthenticationFailed('Token 无效或已过期')
        
        try:
            user = User.objects.get(id=payload.get('user_id'))
        except User.DoesNotExist:
            raise exceptions.AuthenticationFailed('用户不存在')
        
        return (user, None)
```

---

## 六、数据库操作

### 6.1 Django ORM 查询

```python
from .models import Project, TestCase

# 基本查询
projects = Project.objects.all()
project = Project.objects.get(id=1)

# 过滤查询
active_projects = Project.objects.filter(status='active')
my_projects = Project.objects.filter(owner=request.user)

# 关联查询
test_cases = TestCase.objects.filter(module__version__repository__project=project)

# 聚合查询
from django.db.models import Count, Q

stats = Project.objects.aggregate(
    total=Count('id'),
    active=Count('id', filter=Q(status='active'))
)

# 排序
projects = Project.objects.order_by('-created_at')

# 分页
from django.core.paginator import Paginator

paginator = Paginator(projects, 10)
page = paginator.get_page(1)
```

### 6.2 数据库迁移

```bash
# 创建迁移文件
python manage.py makemigrations

# 执行迁移
python manage.py migrate

# 查看迁移状态
python manage.py showmigrations

# 回滚迁移
python manage.py migrate app_name migration_name
```

---

## 七、测试

### 7.1 前端测试

```javascript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import LoginPage from '@/views/auth/LoginPage.vue'

describe('LoginPage', () => {
  it('renders properly', () => {
    const wrapper = mount(LoginPage)
    expect(wrapper.find('form').exists()).toBe(true)
  })
})
```

### 7.2 后端测试

```python
from django.test import TestCase
from django.urls import reverse
from apps.users.models import User

class UserAuthTestCase(TestCase):
    def setUp(self):
        self.user = User.objects.create_user(
            username='testuser',
            email='test@example.com',
            password='testpass123',
            name='Test User'
        )
    
    def test_login_success(self):
        response = self.client.post(reverse('login'), {
            'username': 'testuser',
            'password': 'testpass123'
        }, content_type='application/json')
        self.assertEqual(response.status_code, 200)
        self.assertIn('token', response.json()['data'])
    
    def test_login_invalid_credentials(self):
        response = self.client.post(reverse('login'), {
            'username': 'testuser',
            'password': 'wrongpass'
        }, content_type='application/json')
        self.assertEqual(response.status_code, 401)
```

---

## 八、调试技巧

### 8.1 前端调试

```javascript
// Vue DevTools
// 安装 Vue DevTools 浏览器扩展

// Console 调试
console.log('Debug:', variable)

// Debugger 断点
debugger

// Vue 组件调试
import { getCurrentInstance } from 'vue'

const instance = getCurrentInstance()
console.log(instance)
```

### 8.2 后端调试

```python
# Django Shell
python manage.py shell

# 日志配置
import logging
logger = logging.getLogger(__name__)
logger.error(f"Debug info: {variable}")

# 断点调试
import pdb; pdb.set_trace()

# 或使用 ipdb
import ipdb; ipdb.set_trace()
```

---

## 九、常见问题

### 9.1 前端问题

| 问题 | 解决方案 |
|------|----------|
| 跨域错误 | 检查后端 CORS 配置 |
| Token 失效 | 检查 Token 过期时间，重新登录 |
| 路由 404 | 检查路由配置和路径 |
| 组件不更新 | 检查响应式数据是否正确 |

### 9.2 后端问题

| 问题 | 解决方案 |
|------|----------|
| 数据库迁移失败 | 删除迁移文件重新生成 |
| CORS 错误 | 检查 CORS 配置 |
| 认证失败 | 检查 Token 是否有效 |
| 500 错误 | 查看服务器日志 |

---

## 十、代码提交规范

### 10.1 Commit Message 格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 10.2 Type 类型

| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | 修复 Bug |
| docs | 文档更新 |
| style | 代码格式调整 |
| refactor | 代码重构 |
| test | 测试相关 |
| chore | 构建/工具相关 |

### 10.3 示例

```
feat(projects): 添加项目成员批量导入功能

- 支持Excel文件导入
- 支持CSV文件导入
- 添加导入进度显示

Closes #123
```

---

## 十一、修订历史

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| V1.0 | 2024 | 开发团队 | 初始版本 |
